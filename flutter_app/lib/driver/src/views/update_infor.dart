import 'package:flutter/material.dart';
import '../../models/driver.dart';

class UpdateInfor extends StatelessWidget {
  final Driver driver;
  UpdateInfor({required this.driver});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
